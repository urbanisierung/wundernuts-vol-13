# Wundernut13 Frontend

Frontend that illustrates the wundernut solution.

## dev

```bash
# start locally
npm run start

# build and start build
npm run build && cd ./dist && http-server -p 4200
```
